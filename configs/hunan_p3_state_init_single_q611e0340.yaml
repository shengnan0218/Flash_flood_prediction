_base_: hunan_p3_state_init.yaml

# Formal single-basin P3 probe. The graph restriction is supplied explicitly
# by the training command; all continuous TRAIN/VALIDATION/TEST semantics,
# Q+Z multitask supervision, TRAIN weighted sampling, optimizer, LR, batch
# size, physical equations, and 100-epoch budget remain inherited unchanged.
training:
  checkpoint: outputs/hunan_p3_state_init_single_q611e0340_best.pt
  final_checkpoint: outputs/hunan_p3_state_init_single_q611e0340_epoch100.pt
  log_csv: outputs/hunan_p3_state_init_single_q611e0340_train.csv

hyperparameter_optimization:
  enabled: false
