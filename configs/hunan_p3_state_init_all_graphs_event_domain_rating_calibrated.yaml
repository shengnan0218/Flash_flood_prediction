_base_: hunan_p3_state_init_single_q611e0340_event_domain_rating_calibrated.yaml

# All-graph P3 calibrated-rating baseline.
# Scientific model/training settings are inherited unchanged from the calibrated
# single-basin experiment. This config only removes the single-graph filter and
# isolates outputs for the formal all-domain run.
data:
  graph_id: null
  future_rainfall_mode: persistence

training:
  checkpoint: outputs/hunan_p3_state_init_all_graphs_event_domain_rating_calibrated_best.pt
  final_checkpoint: outputs/hunan_p3_state_init_all_graphs_event_domain_rating_calibrated_epoch100.pt
  log_csv: outputs/hunan_p3_state_init_all_graphs_event_domain_rating_calibrated_train.csv

hyperparameter_optimization:
  enabled: false
